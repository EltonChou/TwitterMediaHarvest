{
  "name": "@media-harvest-gh-action/create-gecko-release",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "clean": "rimraf dist",
    "test": "yarn build && node test/run-tests.mjs",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "semver": "^7.7.1"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.1",
    "@vercel/ncc": "^0.36.0",
    "prettier": "^3.4.2",
    "rimraf": "^5.0.0",
    "typescript": "^5.7.3"
  }
}
